// Banner de verificación de email desactivado temporalmente.
// Para reactivar: restaurar este archivo desde git (commit anterior tiene
// la implementación completa con resend + dismiss localStorage).
export default function VerifyEmailBanner() {
  return null;
}
